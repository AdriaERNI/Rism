{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Rism","text":"<p>Prism with Rust \u2014 a fast, single-binary companion for InterSystems IRIS: a command-line tool and an MCP server (Model Context Protocol, for AI agents) that talk to your IRIS instance over the Atelier REST API. No server-side code is ever uploaded by Rism itself, no agents, no <code>%Studio</code> globals \u2014 everything travels through the documented REST + WebSocket endpoints your IRIS already exposes.</p> <p> </p>"},{"location":"#why-rism","title":"Why Rism","text":"Rism Distribution one static binary (Linux, Windows, macOS) \u2014 no Python, no runtime installs Two doors, one core <code>rism &lt;command&gt;</code> for humans and scripts; <code>rism mcp</code> for AI clients (Claude Desktop, Hermes, any MCP client) Zero footprint on the server every tool rides the Atelier API (<code>/api/atelier/\u2026</code>) and the terminal WebSocket \u2014 nothing is installed inside IRIS Prism parity behavior-matched port of Prism's 25 tools, log format, and debugger, in ~8k lines of Rust Debugger without Studio full DBGP control (breakpoints, stepping, variable inspection) from the CLI or an agent session \u2014 no GUI needed"},{"location":"#25-tools-at-a-glance","title":"25 tools at a glance","text":"<p>Documents \u00b7 SQL \u00b7 terminal \u00b7 tests \u00b7 debugger \u00b7 monitoring \u00b7 host files. See MCP Tools for the full reference with parameters and examples.</p>"},{"location":"#quick-start","title":"Quick start","text":"<pre><code># 1. point Rism at your IRIS (env vars or a config file \u2014 see Configuration)\nexport RISM_IRIS_BASE_URL=http://localhost:52773\nexport RISM_IRIS_USERNAME=_SYSTEM\nexport RISM_IRIS_PASSWORD=***   # never stored in the config file\n\n# 2. smoke test\nrism info                      # version + namespaces =&gt; connectivity OK\n\n# 3. work\nrism sql \"SELECT TOP 5 Name FROM %Dictionary.CompiledClass WHERE NameSpace='USER'\"\nrism doc compile MyPackage.MyClass.cls\nrism test run MyPackage.MyClassTest\nrism debug run '##class(MyPackage.MyClass).Main()' --stop-on-entry\nrism monitor                   # live metrics + load score\n</code></pre> <p>Serving as an MCP server for an AI client is one line in your client config:</p> <pre><code>{ \"mcpServers\": { \"rism\": { \"command\": \"rism\", \"args\": [\"mcp\"] } } }\n</code></pre>"},{"location":"#where-to-go-next","title":"Where to go next","text":"<ul> <li>Getting Started \u2014 install, first connection, compose.yaml for a local IRIS</li> <li>Configuration \u2014 config file, environment variables, precedence</li> <li>CLI Reference \u2014 every command and flag</li> <li>MCP Tools \u2014 all 25 tools with parameters</li> <li>Debugger \u2014 DBGP sessions, breakpoints, stepping</li> <li>Development \u2014 build, test, CI, release flow</li> <li>Windows Store \u2014 installer, Store submission, readiness checklist</li> </ul>"},{"location":"CHANGELOG/","title":"Changelog","text":"<p>All notable changes to Rism. Versioning follows Git Flow: <code>release/vX.Y.Z</code> \u2192 <code>main</code> \u2192 tag.</p>"},{"location":"CHANGELOG/#010-unreleased","title":"0.1.0 \u2014 unreleased","text":""},{"location":"CHANGELOG/#added","title":"Added","text":"<ul> <li>Core: MCP server (<code>rism mcp</code>, rmcp over stdio) + full CLI (<code>rism &lt;cmd&gt;</code>)   sharing one implementation \u2014 25 tools over the Atelier REST API, zero   server-side code uploads.</li> <li>Documents: <code>list/get/put/delete</code>, upload-without-compile (<code>put_document</code>),   upload+compile (<code>put_and_compile</code>), compile-in-place (<code>compile_documents</code>).</li> <li>SQL &amp; terminal: <code>execute_sql</code> (capped, JSON or table), <code>execute_command</code>   over the terminal WebSocket.</li> <li>Unit tests: <code>run_tests</code> / <code>list_tests</code> / <code>get_test_results</code> via   <code>%UnitTest.Manager</code>.</li> <li>Debugger: DBGP over the Atelier WebSocket \u2014 9 <code>debug_*</code> tools +   <code>rism debug run|ps|attach</code>; frame reassembly, <code>\\n</code>-terminated commands,   stack-before-context ordering, 4\u00d7 connect retry. <code>RISM_DEBUG_TOOLS=0</code> hides   them from discovery.</li> <li>Monitoring: <code>monitor_system</code> scored snapshot (overall + cpu/memory/disk/   process, units in output) + <code>rism monitor</code>.</li> <li>Host tools: <code>read_file</code> / <code>list_files</code> / <code>run_shell</code> rooted at   <code>RISM_WORKSPACE</code> with traversal refusal (PowerShell on Windows).</li> <li>Windows: Inno Setup installer (<code>installer/rism.iss</code>), per-machine   install + PATH task; CI job installs/upgrades/uninstalls it on   <code>windows-latest</code> against a live IRIS.</li> <li>CI: lint (fmt + clippy <code>-D warnings</code>), unit tests, fresh-IRIS live smoke,   tagged release builds (Linux/Windows/macOS + installer), GitHub Pages.</li> <li>Docs: this site (<code>docs/</code>, mkdocs-material).</li> </ul>"},{"location":"CHANGELOG/#parity-notes","title":"Parity notes","text":"<ul> <li>Log format is byte-compatible with Prism's (<code>src/logfmt.rs</code>).</li> <li><code>test list --filter</code> is a starts-with prefix (no <code>%</code> wildcards) \u2014 same   behavior as Prism's discovery query.</li> </ul>"},{"location":"cli-reference/","title":"CLI Reference","text":"<p>Every command works against the configured IRIS instance (Configuration). Common flags on nearly all commands:</p> Flag Meaning <code>--url &lt;URL&gt;</code> override <code>iris_base_url</code> for this call <code>--namespace &lt;NS&gt;</code> override the default namespace <code>--format &lt;FMT&gt;</code> <code>text</code> (default) or <code>json</code> \u2014 parse-friendly output <code>-V, --version</code> print version"},{"location":"cli-reference/#connection","title":"Connection","text":""},{"location":"cli-reference/#rism-info","title":"<code>rism info</code>","text":"<p>Server info + connectivity smoke test (product, Atelier API version, namespaces). No arguments.</p>"},{"location":"cli-reference/#sql","title":"SQL","text":""},{"location":"cli-reference/#rism-sql-query","title":"<code>rism sql &lt;QUERY&gt;</code>","text":"<p>Run one SQL statement.</p> <pre><code>rism sql \"SELECT TOP 10 Name FROM %Dictionary.CompiledClass\"\nrism sql --max-rows 50 --format json \"SELECT * FROM INFORMATION_SCHEMA.TABLES\"\n</code></pre> <p>Flags: <code>--max-rows &lt;N&gt;</code> (default 1000).</p>"},{"location":"cli-reference/#documents","title":"Documents","text":""},{"location":"cli-reference/#rism-doc-list","title":"<code>rism doc list</code>","text":"<p>List documents. Flags: <code>--filter &lt;PREFIX&gt;</code> (starts-with on name), <code>--filetypes &lt;LIST&gt;</code>, <code>--count &lt;N&gt;</code>.</p> <pre><code>rism doc list --filter MyApp --filetypes cls --count 50\n</code></pre>"},{"location":"cli-reference/#rism-doc-get-name","title":"<code>rism doc get &lt;NAME&gt;</code>","text":"<p>Fetch source. Flags: <code>--format</code> (<code>text</code>/<code>json</code>), <code>--save &lt;PATH&gt;</code> (write to a local file instead of stdout \u2014 pairs with <code>doc put</code> for read\u2192edit\u2192put loops).</p> <pre><code>rism doc get MyApp.Hello.cls --save ./MyApp.Hello.cls\n</code></pre>"},{"location":"cli-reference/#rism-doc-put-name-file-path","title":"<code>rism doc put &lt;NAME&gt; --file &lt;PATH&gt;</code>","text":"<p>Upload without compiling (Prism parity: compile is explicit). Flags: <code>--format</code>, <code>--no-ignore-conflict</code> (fail on timestamp conflict instead of overwriting).</p> <pre><code>rism doc put MyApp.Hello.cls --file ./MyApp.Hello.cls\n</code></pre>"},{"location":"cli-reference/#rism-doc-compile-name-file-path","title":"<code>rism doc compile &lt;NAME&gt; --file &lt;PATH&gt;</code>","text":"<p>Upload and compile in one call. Flags as <code>put</code>, plus <code>--flags &lt;LIST&gt;</code> for compile flags.</p>"},{"location":"cli-reference/#rism-doc-delete-name","title":"<code>rism doc delete &lt;NAME&gt;</code>","text":"<p>Delete a document from the server.</p>"},{"location":"cli-reference/#rism-compile-names","title":"<code>rism compile &lt;NAMES&gt;...</code>","text":"<p>Compile documents that are already on the server \u2014 no upload.</p> <pre><code>rism compile MyApp.Hello.cls MyApp.Other.cls\n</code></pre>"},{"location":"cli-reference/#terminal-objectscript","title":"Terminal / ObjectScript","text":""},{"location":"cli-reference/#rism-exec-command","title":"<code>rism exec &lt;COMMAND&gt;</code>","text":"<p>Run an ObjectScript command in an IRIS terminal session over the terminal WebSocket. Quoting note: single-quote the argument so <code>$</code> sequences survive to IRIS; escape a real double quote as <code>\\\"</code> inside it.</p> <pre><code>rism exec 'write \"1+1=\", 1+1'\nrism exec 'do ##class(MyApp.Hello).Greet()'\n</code></pre> <p>Flags: <code>--format</code>.</p>"},{"location":"cli-reference/#unit-tests","title":"Unit tests","text":""},{"location":"cli-reference/#rism-test-run-class","title":"<code>rism test run &lt;CLASS&gt;</code>","text":"<p>Run a <code>%UnitTest</code> procedure block via the manager (nothing is uploaded). Flags: <code>--method &lt;NAME&gt;</code> (single test method), <code>--timeout &lt;SECS&gt;</code>.</p> <pre><code>rism test run MyApp.MyClassTest\nrism test run MyApp.MyClassTest --method TestHello\n</code></pre>"},{"location":"cli-reference/#rism-test-list","title":"<code>rism test list</code>","text":"<p>Discover <code>%UnitTest</code> classes. Flag: <code>--filter &lt;PREFIX&gt;</code> \u2014 a starts-with prefix (SQL LIKE <code>%</code> is added internally; do not append one).</p>"},{"location":"cli-reference/#rism-test-results","title":"<code>rism test results</code>","text":"<p>Stored results history, newest first. Flags: <code>--limit &lt;N&gt;</code>, <code>--class &lt;NAME&gt;</code> filter.</p>"},{"location":"cli-reference/#debugger","title":"Debugger","text":"<p>One-shot helpers around the DBGP session engine (details in Debugger):</p> <pre><code>rism debug ps                              # list debuggable processes\nrism debug run '##class(MyApp.Svc).Main()' --stop-on-entry\nrism debug attach &lt;pid&gt;\n</code></pre> <p><code>debug run</code> flags: <code>--stop-on-entry</code>, <code>--class &lt;CLS&gt;</code>, <code>--method &lt;M&gt;</code>, plus the target as a positional expression.</p>"},{"location":"cli-reference/#host-tools","title":"Host tools","text":"<p>Local-file and local-shell tools for agents that edit code on the machine running Rism. Active only with <code>RISM_WORKSPACE</code> / <code>workspace_root</code> set; traversal outside the root is refused.</p> <pre><code>rism shell \"cargo test\" --cwd ./proj       # bash here, PowerShell on Windows\nrism cat src/main.rs\nrism ls \"src/**/*.rs\"\n</code></pre>"},{"location":"cli-reference/#monitoring","title":"Monitoring","text":""},{"location":"cli-reference/#rism-monitor","title":"<code>rism monitor</code>","text":"<p>Live metrics snapshot with per-category scores (overall + cpu/memory/disk/process), units included. <code>--raw</code> prints the raw metric vector instead of the scored table.</p>"},{"location":"cli-reference/#mcp-server","title":"MCP server","text":""},{"location":"cli-reference/#rism-mcp","title":"<code>rism mcp</code>","text":"<p>Serve JSON-RPC 2.0 over stdio (all 25 tools). Used by MCP clients; no flags. <code>RISM_DEBUG_TOOLS=0</code> removes the 9 <code>debug_*</code> tools from discovery.</p>"},{"location":"configuration/","title":"Configuration","text":"<p>Rism resolves settings with a simple precedence chain:</p> <pre><code>environment variables  &gt;  config file  &gt;  built-in defaults\n</code></pre>"},{"location":"configuration/#config-file","title":"Config file","text":"<p>Location (per OS):</p> OS Path Linux <code>~/.config/rism/config.toml</code> (honors <code>$XDG_CONFIG_HOME</code>) macOS <code>~/Library/Application Support/rism/config.toml</code> Windows <code>%APPDATA%\\rism\\config.toml</code> <p>Example:</p> <pre><code>iris_base_url  = \"http://localhost:52773\"\niris_username  = \"_SYSTEM\"\niris_namespace = \"USER\"\niris_api_version = 0        # 0 = negotiate from GET /api/atelier/\ntimeout_secs   = 30\nsql_max_rows   = 1000\nterminal_max_output_chars = 100000\nworkspace_root = \"~/code\"   # root for read_file/list_files/run_shell\ndebug_tools_enabled = true\n</code></pre> <p>The password does not live here</p> <p><code>iris_password</code> is deliberately skipped by the config parser (<code>#[serde(skip)]</code>). It can only come from <code>RISM_IRIS_PASSWORD</code>. A password in the file is silently ignored \u2014 by design, so config files are safe to commit, sync, or paste into bug reports. The same applies to <code>rism</code>'s own display/logging: it redacts the password everywhere.</p>"},{"location":"configuration/#environment-variables","title":"Environment variables","text":"Variable Field Default <code>RISM_IRIS_BASE_URL</code> Atelier base URL <code>http://localhost:52773</code> <code>RISM_IRIS_USERNAME</code> username <code>_SYSTEM</code> <code>RISM_IRIS_PASSWORD</code> password <code>SYS</code> (IRIS factory default) <code>RISM_IRIS_NAMESPACE</code> default namespace <code>USER</code> <code>RISM_IRIS_API_VERSION</code> API prefix (0 = negotiate) <code>0</code> <code>RISM_WORKSPACE</code> root for host file tools (unset = disabled) <code>RISM_DEBUG_TOOLS</code> <code>0</code> hides the 9 <code>debug_*</code> tools <code>1</code> <p><code>RISM_WORKSPACE</code> also applies to the CLI (<code>rism cat</code>, <code>rism ls</code>, <code>rism shell</code>); without it those tools refuse to run rather than defaulting to <code>/</code>.</p>"},{"location":"configuration/#defaults","title":"Defaults","text":"<p>Everything unset gives: <code>http://localhost:52773</code>, <code>_SYSTEM</code>/<code>SYS</code>, <code>USER</code>, API negotiation, 30 s timeout, SQL capped at 1 000 rows, terminal output capped at 100 000 chars, debugger enabled, host file tools disabled until you set a workspace root.</p>"},{"location":"configuration/#per-invocation-overrides","title":"Per-invocation overrides","text":"<p>CLI connection flags beat both file and env for a single command:</p> <pre><code>rism --url https://iris-prod.internal:443 --namespace PROD info\n</code></pre> <p>MCP tools accept an optional <code>namespace</code> parameter per call for the same effect.</p>"},{"location":"configuration/#multiple-environments","title":"Multiple environments","text":"<p>Because precedence is env &gt; file, keep one config file with your dev instance and wrap prod in an env block (shell profile, systemd unit, MCP client <code>env</code>). On Windows the Store build reads the same <code>%APPDATA%\\rism\\config.toml</code>, so a config written by the CLI works unchanged in the MCP server.</p>"},{"location":"configuration/#where-to-go-next","title":"Where to go next","text":"<ul> <li>CLI Reference</li> <li>MCP Tools</li> </ul>"},{"location":"debugger/","title":"Debugger","text":"<p>Rism drives IRIS's DBGP debugger through the Atelier API \u2014 over a WebSocket tunnel to <code>/api/atelier/\u2026/debug</code>. No GUI, no Visual Studio, no server-side debug agents beyond what IRIS ships. Behavior is 1:1 with Prism's debugger.</p>"},{"location":"debugger/#concepts","title":"Concepts","text":"<ul> <li>A debug session wraps one DBGP connection: Rism asks IRIS to launch (or   attach to) a job, then speaks DBGP commands (<code>breakpoint_set</code>, <code>run</code>,   <code>step_over</code>, <code>context_get</code>, <code>property_get</code>, <code>stack_get</code>, \u2026).</li> <li>Sessions have an id (returned by <code>debug_start</code>/<code>debug_attach</code>); the other   eight <code>debug_*</code> tools take <code>session_id</code>.</li> <li>A stop suspends the target job. Always end sessions with <code>debug_stop</code> /   <code>debug_step action=run</code> \u2014 a crashed client that leaves a session open pauses   the job until it times out. (If a job is orphaned this way, <code>docker restart</code>   or terminating the job is the reliable recovery \u2014 <code>debug_stop</code> from a new   session does not reclaim the old one.)</li> </ul>"},{"location":"debugger/#cli-run-to-completion-with-stops","title":"CLI: run to completion with stops","text":"<pre><code>rism debug ps                              # candidate jobs (pid, namespace, state)\nrism debug run '##class(MyApp.Svc).Main()' --stop-on-entry\n</code></pre> <p>Prints each stop (<code>stop 1 at MyApp.Svc.cls:12</code>) and a summary \u2014 the fast way to see where execution actually goes.</p> <pre><code>rism debug attach &lt;pid&gt;                    # snapshot current location, resume\n</code></pre>"},{"location":"debugger/#mcp-step-by-step-control","title":"MCP: step-by-step control","text":"<pre><code>// start at entry with a breakpoint\ndebug_start { \"target\": \"##class(MyApp.Svc).Main()\", \"stop_on_entry\": true,\n              \"breakpoints\": [{ \"class\": \"MyApp.Svc\", \"method\": \"Check\",\n                                 \"offset\": 3 }] }\n// \u2192 { \"session_id\": \"1\", \"stop\": { \"file\": \"MyApp.Svc.cls\", \"line\": 1 } }\n\ndebug_stack     { \"session_id\": \"1\" }\ndebug_variables { \"session_id\": \"1\", \"context\": \"private\" }\ndebug_inspect   { \"session_id\": \"1\", \"expression\": \"$zobjclass(\\\"%Library.String\\\")\" }\ndebug_step      { \"session_id\": \"1\", \"action\": \"step_over\" }\ndebug_breakpoints { \"session_id\": \"1\", \"action\": \"enable\", \"id\": 1 }\ndebug_stop      { \"session_id\": \"1\" }\n</code></pre>"},{"location":"debugger/#protocol-notes-what-rism-handles-for-you","title":"Protocol notes (what Rism handles for you)","text":"<ul> <li>Frames arrive as <code>len|base64</code> chunks and are reassembled before XML parsing.</li> <li>Every WS text command is <code>\\n</code>-terminated \u2014 IRIS's DBGP agent never replies   otherwise.</li> <li><code>context_get</code> crashes the agent unless <code>stack_get</code>/<code>feature_set</code> ran first;   Rism always establishes the stack before reading variables.</li> <li>Connects are retried (4\u00d7) against transient <code>#6713</code> session-slot races.</li> <li><code>xmltree</code> owns the parsed reply, so extracted values outlive the parse call.</li> </ul>"},{"location":"debugger/#caveats","title":"Caveats","text":"<ul> <li>One active debug session per connection; <code>debug_stop</code> frees it.</li> <li>Stepping through compiled <code>%SYS</code> classes works but stops are server-side \u2014   keep sessions short on production jobs.</li> </ul>"},{"location":"development/","title":"Development","text":""},{"location":"development/#build-test-locally","title":"Build &amp; test locally","text":"<pre><code>cargo build                          # debug binary: target/debug/rism\ncargo test                           # 47 unit + wiremock API tests \u2014 no server needed\ncargo clippy --all-targets -- -D warnings\ncargo fmt\n</code></pre> <p>The test suite mocks the Atelier API with <code>wiremock</code> (<code>tests/api.rs</code>), so unit/CI tests never need a live IRIS. A couple of tests exercise the real DBGP parser against captured frames.</p>"},{"location":"development/#live-smoke-against-fresh-iris","title":"Live smoke against fresh IRIS","text":"<p>The <code>Live smoke (fresh IRIS)</code> CI job boots <code>intersystemsdc/iris-community:2025.3</code> as a service container and drives the shipped binary through every door \u2014 <code>info</code>, <code>doc put</code>/<code>compile</code>/<code>get</code>/<code>delete</code>, <code>sql</code>, <code>exec</code>, <code>test run/list/results</code>, <code>debug run</code> (real DBGP stop), <code>monitor</code>. Its purpose: catch anything a dev box masks (pre-compiled caches, leftover classes). Local equivalent:</p> <pre><code>docker compose up -d            # rism-iris on :52773\nexport RISM_IRIS_BASE_URL=http://localhost:52773\n./target/debug/rism info\n</code></pre> <p>Fresh-server lessons baked into the workflow (keep them):</p> <ul> <li><code>doc put</code> does not compile \u2014 test classes need an explicit <code>rism compile</code>   before <code>test run</code>, or the suite reports them missing.</li> <li><code>%UnitTest</code> discovery needs compiled classes \u2014 <code>test list</code> returns 0 on an   empty server; filter by class prefix, not <code>%</code> LIKE patterns.</li> <li>DBGP WS frames are <code>\\n</code>-terminated and <code>context_get</code> requires a prior   <code>stack_get</code> \u2014 see Debugger.</li> </ul>"},{"location":"development/#ci","title":"CI","text":"Workflow Trigger Gates <code>ci.yml</code> \u2192 Lint PR / push <code>cargo fmt --check</code>, <code>clippy -D warnings</code> <code>ci.yml</code> \u2192 Test PR / push <code>cargo test --locked</code> (Linux + Windows) <code>ci.yml</code> \u2192 Live smoke (fresh IRIS) PR / push end-to-end on clean 2025.3 <code>ci.yml</code> \u2192 Build + GitHub Release tag <code>v*</code> signed binaries, winx64/setup.exe, checksums <code>pages.yml</code> push <code>main</code> <code>mkdocs build --strict</code> \u2192 GitHub Pages <p>Note: clippy on GitHub's current stable flags stricter lints than pinned toolchains locally (<code>useless_format</code>, <code>items_after_statements</code>, missing <code>must_use</code>) \u2014 run <code>cargo clippy --all-targets -- -D warnings</code> on your latest stable before pushing.</p>"},{"location":"development/#git-flow","title":"Git Flow","text":"<ul> <li><code>development</code> \u2014 daily work, always green.</li> <li><code>release/vX.Y.Z</code> \u2014 cut from <code>development</code>; version bump + <code>docs/CHANGELOG.md</code>   entry happen here.</li> <li>PR <code>release/vX.Y.Z</code> \u2192 <code>main</code>; merge (web UI) \u2192 tag <code>vX.Y.Z</code> \u2192 CI builds all   platforms and publishes the GitHub Release.</li> <li>After release: <code>main</code> is hard-reset onto <code>development</code> (trees identical \u2014 not   a rebase), so <code>development</code> contains the release commits.</li> </ul> <p>Never <code>gh release create</code> \u2014 the workflow owns releases.</p>"},{"location":"development/#adding-a-tool","title":"Adding a tool","text":"<ol> <li>Implement in <code>src/tools/</code> mirroring the Prism module (parity first);    register in <code>src/mcp/mod.rs</code>.</li> <li>Wiremock test in <code>tests/api.rs</code> + a CLI alias if it's a user-facing door    (<code>src/cli/</code>).</li> <li>If it touches the server: add it to the live-smoke step in <code>ci.yml</code>.</li> <li>Document in <code>docs/mcp-tools.md</code> \u2014 read the <code>JsonSchema</code> derive, don't guess.</li> </ol>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#requirements","title":"Requirements","text":"<ul> <li>A reachable InterSystems IRIS (or HealthShare/IRIS for Health) instance with the   Atelier API enabled. Rism's CI smoke-tests against IRIS Community 2025.3;   any version exposing <code>/api/atelier/</code> v1\u2013v4 works.</li> <li>Credentials for an account with <code>%All</code> or at least Studio/Atelier web-service   permissions (<code>%Service_Studio</code>, <code>%Service_CSP</code>).</li> </ul> <p>Rism itself needs nothing else: a single binary, no runtime, no database client, no ODBC/DSN.</p>"},{"location":"getting-started/#install","title":"Install","text":"<p>=== From a release ===</p> <p>Download the archive for your platform from the releases page and put <code>rism</code> on your PATH:</p> <ul> <li>Linux / macOS: <code>rism-&lt;version&gt;-&lt;os&gt;.tar.gz</code> \u2192 extract, <code>chmod +x rism</code></li> <li>Windows: <code>rism-&lt;version&gt;-windows.zip</code> \u2192 extract, or install with   <code>rism-&lt;version&gt;-setup.exe</code> (adds to PATH, Start-menu entry; see   Windows Store)</li> </ul> <p>=== From source ===</p> <pre><code>git clone https://github.com/AdriaERNI/Rism\ncd Rism\ncargo install --path .\n</code></pre> <p>Rust 1.85+ (edition 2024) is the only build-time requirement.</p>"},{"location":"getting-started/#first-connection","title":"First connection","text":"<p>Set three environment variables (or write the config file \u2014 see Configuration):</p> <pre><code>export RISM_IRIS_BASE_URL=http://localhost:52773   # your IRIS web server\nexport RISM_IRIS_USERNAME=_SYSTEM\nexport RISM_IRIS_PASSWORD=***   # read from the environment only, never the config file\n</code></pre> <p>Verify:</p> <pre><code>$ rism info\nIRIS for UNIX (Ubuntu Server ... 2025.3)  ... on x86-64\nAtelier API: v4\nNamespaces: USER, %SYS, ...\n</code></pre> <p>That is a full round trip: discovery (<code>/api/atelier/</code>), auth (Basic), and namespace listing. If it prints, everything else works.</p>"},{"location":"getting-started/#local-iris-with-docker-optional","title":"Local IRIS with Docker (optional)","text":"<p>A <code>compose.yaml</code> ships in the repo for a throwaway instance:</p> <pre><code>docker compose up -d          # intersystemsdc/iris-community:2025.3, web on :52773\ndocker exec -it rism-iris iris session iris -U USER\n</code></pre> <p>The web server listens on 52773 in the Community image (mapped 1:1 by the compose file and both CI jobs), so <code>http://localhost:52773</code> is the base URL. The CI job uses the exact same image, so behavior you see locally is what CI verifies.</p>"},{"location":"getting-started/#a-first-session","title":"A first session","text":"<pre><code>rism doc put MyApp.Hello.cls --file hello.cls   # upload, do NOT compile yet\nrism compile MyApp.Hello.cls                     # compile when ready\nrism exec 'write \"hi\"'                           # run ObjectScript in the terminal\nrism sql \"select top 3 * from %SYS.ProcessQuery\" # query anything\nrism doc delete MyApp.Hello.cls                  # clean up\n</code></pre>"},{"location":"getting-started/#use-with-an-ai-agent-mcp","title":"Use with an AI agent (MCP)","text":"<p>Run <code>rism mcp</code> as a stdio server in any MCP client:</p> <pre><code>{\n  \"mcpServers\": {\n    \"rism\": {\n      \"command\": \"rism\",\n      \"args\": [\"mcp\"],\n      \"env\": {\n        \"RISM_IRIS_BASE_URL\": \"http://localhost:52773\",\n        \"RISM_IRIS_PASSWORD\": \"&lt;from your secret manager&gt;\"\n      }\n    }\n  }\n}\n</code></pre> <p>The agent then sees all 25 tools (MCP Tools). Secrets travel via the client's <code>env</code> block \u2014 never paste passwords into the chat with your agent.</p>"},{"location":"getting-started/#where-to-go-next","title":"Where to go next","text":"<ul> <li>Configuration \u2014 precedence, file reference</li> <li>CLI Reference \u2014 every flag</li> <li>Debugger \u2014 stepping through ObjectScript</li> </ul>"},{"location":"mcp-tools/","title":"MCP Tools","text":"<p>Rism serves 25 tools over JSON-RPC 2.0 / stdio (<code>rism mcp</code>). Parameters marked <code>*</code> are required. All tools accept an optional <code>namespace</code> (override the configured default) unless noted. Every tool's behavior is identical to the CLI path \u2014 same core, two doors.</p> <p>Set <code>RISM_DEBUG_TOOLS=0</code> to remove the 9 <code>debug_*</code> tools from <code>tools/list</code> (attaching pauses live IRIS jobs; some deployments disable them).</p>"},{"location":"mcp-tools/#server-diagnostics","title":"Server &amp; diagnostics","text":""},{"location":"mcp-tools/#get_server_info","title":"<code>get_server_info</code>","text":"<p>Product version, Atelier API version, namespace list. Also the connectivity probe. No parameters.</p>"},{"location":"mcp-tools/#documents","title":"Documents","text":""},{"location":"mcp-tools/#list_documents","title":"<code>list_documents</code>","text":"Param Notes <code>filter</code> name prefix (starts-with) <code>filetypes</code> e.g. <code>[\"cls\"]</code> <code>count</code> max results"},{"location":"mcp-tools/#get_document-name","title":"<code>get_document</code> \u2014 <code>name</code>*","text":"<p>Source as text lines + metadata (ts, owner, size).</p>"},{"location":"mcp-tools/#put_document-name-content","title":"<code>put_document</code> \u2014 <code>name</code>, <code>content</code>","text":"<p>Upload without compiling \u2014 the content comes from the client, nothing is ever generated on the server. Optional: <code>flags</code>, <code>ignore_conflict</code>.</p>"},{"location":"mcp-tools/#put_and_compile-name-content","title":"<code>put_and_compile</code> \u2014 <code>name</code>, <code>content</code>","text":"<p>Upload then compile in one round trip.</p>"},{"location":"mcp-tools/#compile_documents-names","title":"<code>compile_documents</code> \u2014 <code>names</code>*","text":"<p>Compile documents already on the server (zero uploads \u2014 this is the tool for CI-clean servers). Optional <code>flags</code>.</p>"},{"location":"mcp-tools/#delete_document-name","title":"<code>delete_document</code> \u2014 <code>name</code>*","text":""},{"location":"mcp-tools/#sql-terminal","title":"SQL &amp; terminal","text":""},{"location":"mcp-tools/#execute_sql-query","title":"<code>execute_sql</code> \u2014 <code>query</code>*","text":"<p>Optional <code>max_rows</code>.</p>"},{"location":"mcp-tools/#execute_command-command","title":"<code>execute_command</code> \u2014 <code>command</code>*","text":"<p>ObjectScript command in a terminal session over the WebSocket. Optional <code>timeout_secs</code>.</p>"},{"location":"mcp-tools/#unit-tests","title":"Unit tests","text":""},{"location":"mcp-tools/#run_tests-test_class","title":"<code>run_tests</code> \u2014 <code>test_class</code>*","text":"<p>Via <code>%UnitTest.Manager</code>, no code uploaded. Optional <code>test_method</code>, <code>timeout_secs</code>.</p>"},{"location":"mcp-tools/#list_tests","title":"<code>list_tests</code>","text":"<p>Discover <code>%UnitTest</code> classes. <code>filter</code> is a prefix \u2014 <code>%</code> wildcards are rejected by validation (they would be interpreted literally).</p>"},{"location":"mcp-tools/#get_test_results","title":"<code>get_test_results</code>","text":"<p>Stored history, newest first. Optional <code>test_class</code>, <code>max_runs</code>.</p>"},{"location":"mcp-tools/#debugger-9-tools","title":"Debugger (9 tools)","text":"<p>Full DBGP control without leaving the API session \u2014 see Debugger.</p> Tool Params <code>debug_list_processes</code> <code>system</code> (include system-wide jobs) <code>debug_start</code> <code>target</code>*, <code>stop_on_entry</code>, <code>breakpoints</code> <code>debug_attach</code> <code>pid</code>* <code>debug_step</code> <code>session_id</code>*, <code>action</code> (<code>step_into</code>/<code>step_over</code>/<code>step_out</code>/<code>run</code>/<code>break</code>/<code>stop</code>) <code>debug_stack</code> <code>session_id</code>* <code>debug_variables</code> <code>session_id</code>*, <code>context</code> (<code>private</code>/<code>public</code>/<code>class</code>), <code>stack_level</code> <code>debug_inspect</code> <code>session_id</code>*, <code>expression</code>* <code>debug_breakpoints</code> <code>session_id</code>*, <code>action</code> (<code>list</code>/<code>set</code>/<code>remove</code>/<code>enable</code>/<code>disable</code>), <code>breakpoint</code>, <code>id</code> <code>debug_stop</code> <code>session_id</code>*"},{"location":"mcp-tools/#monitoring","title":"Monitoring","text":""},{"location":"mcp-tools/#monitor_system","title":"<code>monitor_system</code>","text":"<p>Scored snapshot: overall + cpu/memory/disk/process, with units and per-category detail. <code>include_raw_metrics</code> returns the raw vector too.</p>"},{"location":"mcp-tools/#host-file-tools-agent-side","title":"Host file tools (agent-side)","text":"<p>Operate on the machine running Rism, rooted at <code>workspace_root</code> / <code>RISM_WORKSPACE</code>. Path traversal outside the root is refused. Disabled when no root is configured.</p> Tool Params <code>read_file</code> <code>path</code>* \u2014 text; binary detected and rejected <code>list_files</code> <code>path</code>, <code>pattern</code> (glob), <code>max_results</code> <code>run_shell</code> <code>command</code>*, <code>cwd</code>, <code>timeout_secs</code> \u2014 <code>bash</code> on Unix, PowerShell on Windows"},{"location":"mcp-tools/#typical-agent-flows","title":"Typical agent flows","text":"<p>Edit a class: <code>get_document</code> \u2192 (agent edits) \u2192 <code>put_and_compile</code> \u2192 <code>execute_sql \"SELECT \u2026 FROM %Compiler.State\"</code> or <code>compile_documents</code> to verify.</p> <p>Debug a failure: <code>run_tests</code> \u2192 on failure, <code>debug_start</code> with the failing method as <code>target</code> + <code>stop_on_entry=true</code> \u2192 <code>debug_stack</code> / <code>debug_variables</code> / <code>debug_inspect</code> at each stop \u2192 <code>debug_stop</code>.</p> <p>Local dev loop: <code>read_file</code> the working copy \u2192 <code>put_document</code> \u2192 iterate \u2192 <code>compile_documents</code> when green.</p>"},{"location":"windows-store/","title":"Windows &amp; Microsoft Store","text":"<p>How Rism ships on Windows: the binary, the installer, the CI gate that proves them, and the current state of Store readiness.</p>"},{"location":"windows-store/#the-binary","title":"The binary","text":"<p><code>rism.exe</code> is a pure-Rust MSVC build (<code>cargo build --release --target x86_64-pc-windows-msvc</code>) \u2014 no Python runtime, no Visual C++ redistributable requirement beyond what ships in Windows. That removes the biggest support class Prism had on Windows (frozen-Python AV false positives, vcredist merges). The build is produced in CI (tag builds: <code>ci.yml</code>, Windows matrix leg).</p>"},{"location":"windows-store/#the-installer","title":"The installer","text":"<p><code>installer/rism.iss</code> \u2014 Inno Setup 6, per-machine install to <code>{autopf}\\Rism</code> with an optional PATH task. Built by CI as <code>rism-&lt;version&gt;-setup.exe</code> and attached to the GitHub Release. Config lives at <code>%APPDATA%\\rism\\config.toml</code> (see Configuration) \u2014 the installer never touches it, so upgrades and uninstall preserve user settings.</p> <p>Layout:</p> <pre><code>installer/\n  rism.iss                      # the script\n  assets/                       # logo.ico + wizard bitmaps\n  static/analyze-install-consistency.ps1\n  static/analyze-docs-drift.ps1\n</code></pre>"},{"location":"windows-store/#the-windows-gate-what-ci-proves-before-a-release","title":"The Windows gate (what CI proves before a release)","text":"<p><code>.github/workflows/windows-installer.yml</code> runs on every PR that touches the installer or the Rust sources:</p> <ol> <li>Consistency (Linux, cheap) \u2014 static scans: <code>.iss</code> references exist,    PATH contract present, no stale vendor strings.</li> <li>Docs drift (Linux, cheap) \u2014 every tool-count claim in the docs matches    the <code>#[tool]</code> definitions in code; every tool name has a section in    <code>docs/mcp-tools.md</code>; config paths match <code>settings.rs</code>; CHANGELOG top ==    <code>Cargo.toml</code>.</li> <li>Real install contract (windows-latest + live IRIS service) \u2014 silent    install \u2192 <code>rism --version</code> \u2192 <code>rism info</code> + <code>rism sql</code> against the fresh    server \u2192 <code>%APPDATA%</code> config pickup \u2192 in-place upgrade \u2192 uninstall \u2192    verify absence (exe, PATH entry, Start-menu). The artifact proven here is    byte-for-byte the artifact the release workflow ships.</li> </ol> <p>Green here is a hard prerequisite for Store submission \u2014 the Store certification report repeats exactly these scenarios.</p>"},{"location":"windows-store/#microsoft-store-current-status","title":"Microsoft Store \u2014 current status","text":"Item Status Installer passes silent install/upgrade/uninstall on real Windows \u2705 automated in CI App has a versioned, reproducible release build \u2705 <code>ci.yml</code> tag builds Publisher identity (Partner Center account, <code>Adria Sanchez</code>) \u23f3 one-time manual setup Store signature (EV cert, or Partner Center submission-time signing) \u23f3 decision pending Win32 packaging (MSIX wrapper) vs unpackaged Store listing \u23f3 decision pending Privacy policy URL, age ratings, data declaration \u23f3 needs a live site URL AGPL-3.0 notice in the Store listing \u23f3 copy is ready (<code>LICENSE</code>)"},{"location":"windows-store/#whats-left-concretely","title":"What's left, concretely","text":"<ol> <li>Sign the installer. Store Desktop-app submissions must be signed.    Two supported paths: an EV code-signing certificate on the build (then    <code>msstore</code> CLI or Partner Center upload), or let Partner Center sign it    at submission (upload the unsigned <code>.exe</code>, Store injects the signature).    CI is already set up so the artifact submitted = the artifact tested.</li> <li>Choose packaging. For a CLI tool the pragmatic listing is a    Win32 (MSIX) package generated once with the Desktop App Installer \u2014    <code>rism.exe</code> becomes launchable-as-command from the Store install folder.    An unpackaged installer submission is simpler but loses auto-update;    CI keeps both options open because the <code>.iss</code> and the raw exe ship side    by side in every release.</li> <li>Store metadata. Screenshots (CLI sessions render well), description    (reuse <code>docs/index.md</code>), privacy-policy URL \u2014 GitHub Pages is already    enabled; add a short <code>docs/privacy.md</code> with \"Rism stores credentials in    <code>%APPDATA%</code>, never phones home\" (true: zero telemetry).</li> <li>Update policy. Releases come from tags; the Store listing updates per    release (same artifact). Document the cadence in <code>docs/development.md</code>    Git Flow section \u2014 already matches: <code>vX.Y.Z</code> tag \u2192 CI \u2192 GitHub Release \u2192    Store submission (<code>msstore upload</code>).</li> </ol>"},{"location":"windows-store/#why-this-is-low-risk","title":"Why this is low-risk","text":"<p>The only store-specific failure modes are signing and metadata \u2014 the binary needs no special capability handling (no COM, no install hooks beyond PATH), which the CI gate has already exercised on the exact runner image Microsoft uses (<code>windows-latest</code>).</p>"},{"location":"windows-store/#quick-links","title":"Quick links","text":"<ul> <li>Workflow: .github/workflows/windows-installer.yml</li> <li>Installer script: <code>installer/rism.iss</code></li> <li>Releases: https://github.com/AdriaERNI/Rism/releases</li> </ul>"}]}